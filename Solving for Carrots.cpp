#include<string.h>
#include<bits/stdc++.h>
using namespace std;

int main(){

    int concur=0, probl=0;
    cin>>concur>>probl;

    if((concur>=1 && concur<=1000) && (probl>=1 && probl<=1000)){

        for(int i=0;i<concur;i++){
            string aux="";
            cin>>aux;
        }
        cout<<probl<<endl;
    }
}
